from flask import render_template
from flyingcolors import app

@app.route('/')
@app.route('/index')
def index():
    user = { 'nickname': 'Sereres' } # fake user
    return render_template("index.html",
            title = 'Home',
            user = user)
